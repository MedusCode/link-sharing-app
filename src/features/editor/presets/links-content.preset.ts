import ILinksContentPreset from '../types/links-content.preset.type';

const linksContentPreset: ILinksContentPreset = {
  texts: {
    heading: 'Customize your links',
    description: 'Add/edit/remove links below and then share all your profiles with the world!',
  },
  buttons: {
    addButtonText: '+ Add new link',
    saveButtonText: 'Save',
  }
}

export default linksContentPreset;
