import ISectionAnimation from '../../../shared/types/section-animation.type';
import { ANIMATION_SECTION_DURATION } from '../../../shared/constants/animations';

const profileAnimationPreset: ISectionAnimation = {
  side: 'right',
  duration: ANIMATION_SECTION_DURATION,
}

export default profileAnimationPreset;
