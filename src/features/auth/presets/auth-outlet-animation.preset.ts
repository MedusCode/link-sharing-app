import IOutletAnimation from '../../../shared/types/outlet-animation.type';

const authOutletAnimationPreset: IOutletAnimation = {
  mode: 'sync',
  initial: false
}

export default authOutletAnimationPreset;
